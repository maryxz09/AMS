programa {
  funcao inicio() {
    inteiro base,altura,area

    escreva("informme a base e altura")
    leia(base)
    leia(altura)
    area=(base*altura) /2

    escreva("area do triangulo e:",area)
  }
}
