programa {
  funcao inicio() {
    inteiro base,altura,area

    escreva("informe a base e altura:")
    leia(base)
    leia(altura)

    area= base*altura

    escreva("a area do retangulo:",area)
  }
}
